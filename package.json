{
  "name": "rosarinho-e-manel",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:regras": "firebase emulators:exec --only firestore --project regras-teste \"node --test tests/regras.test.js\"",
    "test": "firebase emulators:exec --only firestore --project testes \"node --test tests/*.test.js\"",
    "deploy": "node scripts/deploy.mjs",
    "regras": "firebase deploy --only firestore:rules",
    "regras:storage": "firebase deploy --only firestore:rules,storage"
  },
  "dependencies": {
    "firebase": "^12.17.0",
    "react": "^18.3.1",
    "react-dom": "^19.2.8",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@vitejs/plugin-react": "^4.3.4",
    "firebase-tools": "^15.25.1",
    "vite": "^6.0.7"
  }
}
